import React from 'react'
import { Button } from '@components'

const Apps = (): React.ReactElement => {
  return (
    <div>
      <Button disabled border={'1px solid black'} padding={'24px'}>+++</Button>
    </div>
  )
}

export default Apps
